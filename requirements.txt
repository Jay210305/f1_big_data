# ============================================================
# F1 Tyre & Stint Strategy — Python dependencies (venv)
# ============================================================
# torch is NOT listed here: it must be installed separately with
# CUDA wheels for the RTX 5070 Ti (Blackwell, sm_120). See Phase 0.
#
# Install order (from repo root, venv active):
#   1) python -m pip install --upgrade pip
#   2) python -m pip install torch --index-url https://download.pytorch.org/whl/cu128
#   3) python -m pip install -r requirements.txt
#   4) python src/verify_env.py
# ============================================================

# --- Core data (CPU) ---
numpy>=1.26
pandas>=2.2
polars>=1.5
pyarrow>=17.0
duckdb>=1.1
fastparquet>=2024.5.0
zstandard>=0.23

# --- ML (CPU + GPU) ---
scipy>=1.13
scikit-learn>=1.5
xgboost>=2.1
# On Windows, PyPI's wheel is CPU-only. Build from source with USE_GPU=ON.
lightgbm>=4.5
hdbscan>=0.8.38

# --- Notebooks / viz ---
jupyter>=1.0
matplotlib>=3.9
seaborn>=0.13

# --- Interpretability / hyperparameter search ---
shap>=0.46
optuna>=3.6
