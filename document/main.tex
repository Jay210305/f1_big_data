% ============================================================================
% Motor de Estrategia Prescriptiva de Neumáticos y Stints en Fórmula 1
% Pipeline completo de Big Data y Machine Learning sobre telemetría (2021–2025)
% Formato IEEE (conference) — Curso de Big Data
% ============================================================================
\documentclass[conference]{IEEEtran}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish]{babel}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{array}
\usepackage{graphicx}
\usepackage[hidelinks]{hyperref}
\usepackage{caption}
\usepackage{multirow}

% ----------------------------------------------------------------------------
% Metadatos
% ----------------------------------------------------------------------------
\newcommand{\tituloProyecto}{Motor de Estrategia Prescriptiva de Neumáticos
y Stints en Fórmula 1:\\Pipeline completo de Big Data y Machine Learning
sobre telemetría (2021–2025)}
\newcommand{\autores}{[Nombre del estudiante]}
\newcommand{\institucion}{Universidad [Nombre]\\Facultad de [Ingeniería]}

\begin{document}

\title{\tituloProyecto}

\author{\IEEEauthorblockN{\autores}
\IEEEauthorblockA{\institucion}}

\markboth{Curso de Big Data}{}%

\maketitle

% ----------------------------------------------------------------------------
% Resumen
% ----------------------------------------------------------------------------
\begin{abstract}
Se presenta un pipeline completo de fin a fin que convierte 57\,GB de datos
crudos de telemetría de Fórmula 1 (cinco temporadas, $\sim$367\,000 archivos
JSON, 325.5 millones de muestras de alta frecuencia) en un motor de estrategia
prescriptiva de paradas en boxes y gestión de stints. El sistema ejecuta
enteramente en un solo equipo portátil (32\,GB de RAM, RTX~5070~Ti) mediante
un ETL de streaming con límites de memoria que genera un lago de datos
(`lakehouse') en formato Parquet comprimido con ZSTD (16.7\,GB), un almacén
de características doradas (`Gold') con cinéticas de degradación corregidas
por combustible, un regresor de Vida Útil Remanente (RUL) entrenado con
pérdida asimétrica y un clasificador de `cliff' mediante un ensamble
XGBoost+LightGBM con ponderación de clases, un profiler no supervisado
de estilos de conducción (autoencoder + HDBSCAN), y un optimizador
Monte Carlo de estrategia con contexto de escenario y alertas de cliff
con filtrado anti-falso-positivo. En un backtest fuera de muestra sobre 313
puntos de decisión estratégica reales de la temporada 2025, la recomendación
del optimizador mejora la estrategia ejecutada real en el 78.6\% de los casos
con una ganancia pareada mediana de +15\,s.
\end{abstract}

\begin{IEEEkeywords}
Big Data, Fórmula 1, telemetría, machine learning, estrategia de carrera,
degradación de neumáticos, optimización Monte Carlo, Parquet, DuckDB.
\end{IEEEkeywords}

% ============================================================================
% 1. INTRODUCCIÓN
% ============================================================================
\section{Introducción}

La gestión de neumáticos es uno de los factores estratégicos más determinantes
en la Fórmula 1 moderna. Cada decisión de parada en boxes —cuándo detenerse,
qué compuesto elegir y cuántos stints planificar— impacta directamente el
resultado de la carrera. Sin embargo, las estrategias reales ejecutadas por
los equipos están influidas por factores no modelables (información de pista,
comunicación ingeniero-piloto, riesgo percibido) que divergen frecuentemente
de lo óptimo teórico.

Este proyecto construye un motor de estrategia prescriptiva completo que
parte de datos crudos de telemetría para generar recomendaciones cuantitativas
de parada con bandas de confianza. El sistema se organiza en ocho fases
checkpointeadas (Stop-n-Go), desde la exploración del esquema hasta el
backtest fuera de muestra, ejecutándose íntegramente en un equipo personal
con restricciones estrictas de memoria (32\,GB) y almacenamiento (513\,GB SSD).

Los aportes principales del trabajo son:
\begin{enumerate}
  \item Un ETL de streaming con paralelización por sesión que procesa 57\,GB
        de JSON sin cargar ninguna temporada completa en memoria.
  \item Un esquema de etiquetado de `cliff' de neumáticos que separa la
        degradación física del desgaste estratégico, con exclusión estricta
        de fugas de información (`leakage').
  \item Un profiler de estilo de conducción que desentraña la variabilidad
        del conductor de la evolución de la pista mediante un proxy de
        evolución de la pista con lavado por lluvia.
  \item Un optimizador Monte Carlo con regla de alerta compuesta (cliff +
        RUL) que distingue degradación térmica transitoria de desgaste
        físico real.
\end{enumerate}

\subsection{Arquitectura del sistema y reproducibilidad}

El sistema se estructura como siete fases encadenadas, cada una
verificable de forma independiente antes de avanzar a la siguiente
(metodología Stop-n-Go):

{\scriptsize
\begin{verbatim}
JSON crudo (57GB)
 ->[F2] ETL -> Bronze/Silver
    (16.7GB ZSTD, 325.5M filas)
 ->[F3] Gold: ritmo corr. por
    combustible, ventanas rolling
 ->[F4] Mod.1: RUL + cliff
    (XGB/LGBM/LSTM, perd. asim.)
 ->[F5] Mod.2: estilo conductor
    (Autoencoder + HDBSCAN)
 ->[F6] Mod.3: optimizador MC
    ventana parada + monitor
 ->[F7] Backtest vs. real 2025
\end{verbatim}}

Todo el procesamiento se particiona jerárquicamente
(temporada $\rightarrow$ GP $\rightarrow$ sesión $\rightarrow$
conductor $\rightarrow$ stint), con un worker por sesión, de modo que
ninguna temporada se mantiene completa en memoria. El pipeline completo
es reproducible mediante un único orquestador
(\texttt{src/run\_all.py}) que ejecuta las seis etapas
(\texttt{etl}, \texttt{gold}, \texttt{rul}, \texttt{style},
\texttt{degradation}, \texttt{optimizer}) con lógica de
\emph{skip-if-cached}: cada etapa revisa si sus artefactos ya existen
en \texttt{data/} y solo se reejecuta si faltan o si se invoca con
\texttt{-{}-force}. El \texttt{RUNBOOK.md} documenta el comando exacto,
los artefactos generados y el tiempo de cómputo esperado de cada etapa
(tabla~\ref{tab:runbook}).

\begin{table}[!htbp]
\centering
\caption{Etapas del pipeline y tiempos de cómputo aproximados.}
\label{tab:runbook}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Etapa} & \textbf{Artefactos} & \textbf{Tiempo} \\
\midrule
ETL & \texttt{data/bronze}, \texttt{data/silver} & $\sim$11\,min \\
Gold & \texttt{data/gold/*.parquet} & $\sim$6--8\,min \\
RUL/cliff & \texttt{data/models/*rul*, *cliff*} & $\sim$30\,s \\
Estilo & \texttt{driver\_embeddings.parquet} & $\sim$4\,min \\
Degradación & \texttt{degradation\_model.json} & $\sim$30\,s \\
Optimizador & \texttt{phase6\_demo.json} & $\sim$35\,s \\
Backtest & \texttt{phase7\_backtest.json} & $\sim$40\,s \\
\bottomrule
\end{tabular}
\end{table}

Cada fase termina en un punto de control (\emph{checkpoint}) documentado
en \texttt{annotations/phase\_N\_*.md}, donde se registran los comandos
ejecutados, los resultados obtenidos y la decisión Go/No-Go/Rework antes
de habilitar la fase siguiente.

% ============================================================================
% 2. TRABAJOS RELACIONADOS
% ============================================================================
\section{Trabajos Relacionados}

\subsection{Optimización de estrategia de pit stops en motorsport}

La optimización de paradas en boxes se ha abordado principalmente con
programación dinámica y teoría de juegos. Carrasco Heine y
Thraves~\cite{carrasco2023} formulan la selección de vueltas de parada como
un problema de programación dinámica sobre datos de tiempos de vuelta
públicos. Aguad y Thraves~\cite{aguad2024} extienden esa línea a un juego de
Stackelberg de suma cero entre dos pilotos que compiten, capturando
interacciones de adelantamiento —una dimensión competitiva que este trabajo
\textbf{no} modela, ya que el optimizador aquí evalúa un único auto de forma
aislada. Un enfoque alternativo formula el problema como programación
cuadrática entera mixta (MIQP), resuelta en tiempo real a partir de la vuelta
actual~\cite{miqp2024}. Más recientemente, dos trabajos incorporan
aprendizaje: Neumann~\emph{et al.}~\cite{neumann2025learning} combinan un
programa no lineal entero mixto con un agente de aprendizaje por refuerzo
entrenado sobre un modelo de desgaste inspirado en datos de Pirelli, y
Thomas~\emph{et al.}~\cite{thomas2025rl} desarrollan RL explicable para
estrategia de carrera en colaboración con un equipo de F1. A diferencia de
estos trabajos —que operan sobre tiempos de vuelta agregados o simulaciones
de temporada única—, el presente sistema ancla el modelo de degradación
directamente en telemetría de alta frecuencia (G-forces, aire sucio, entrada
térmica) de cinco temporadas completas, y evalúa la recomendación mediante un
backtest pareado sobre decisiones reales, no solo sobre una simulación
interna.

\subsection{Modelado de degradación de neumáticos}

Cappello y Hoegh~\cite{cappello2025tire} proponen un modelo bayesiano de
espacio de estados que estima la degradación latente del neumático a partir
de tiempos de vuelta públicos de la API de FastF1, usando como caso de
estudio una sola carrera (Lewis Hamilton, GP de Austria 2025). Su enfoque
bayesiano produce una cuantificación de incertidumbre más principiada
(distribución posterior completa) que las bandas empíricas p5–p95 basadas en
IQR usadas aquí, pero se limita a una carrera y no incorpora telemetría de
tensión mecánica; en nuestro caso, la falta de significancia estadística
entre compuestos que ellos reportan se explica, en parte, por el tamaño
muestral reducido de una sola carrera frente a las $\sim$50\,000 curvas de
degradación por stint disponibles en el presente lakehouse.

\subsection{Predicción de Vida Útil Remanente (RUL)}

La predicción de RUL proviene principalmente de la literatura de
Prognostics and Health Management (PHM) industrial. El punto de referencia es
el conjunto de datos NASA C-MAPSS y la métrica asimétrica introducida por
Saxena~\emph{et al.}~\cite{saxena2008}, que penaliza exponencialmente las
predicciones tardías frente a las tempranas —métrica que este trabajo
reutiliza (Ecuación 2 de la Sección~IV) para evaluar la RUL de neumáticos.
Hasta donde llega nuestra revisión, esta es la primera aplicación de dicha
métrica, originalmente diseñada para el desgaste de turbinas de motores de
avión, al dominio de la degradación de neumáticos en motorsport. La
comparación de arquitecturas (LSTM vs. XGBoost/LightGBM sobre pérdida
asimétrica) replica hallazgos consistentes de la literatura de PHM: los
modelos recurrentes tienden a ganar en la zona crítica de vida corta, mientras
que los métodos de árboles de gradiente dominan en las métricas agregadas.

\subsection{Perfilado no supervisado de estilo de conducción}

El clustering no supervisado de comportamiento de manejo se ha estudiado
extensamente en el dominio de vehículos de calle, típicamente con datos de
conducción naturalista a baja frecuencia y fines de seguridad vial o
seguros. Wang, Ramesh y Zhao~\cite{wang2018clustering} combinan
autoencoders con medidas basadas en distancia para agrupar encuentros de
conducción a partir de trayectorias GPS, un patrón metodológico similar al
usado aquí (autoencoder + clustering de densidad) pero aplicado a un dominio
de baja frecuencia y sin la restricción de un circuito cerrado. El
clustering de densidad HDBSCAN~\cite{campello2013,mcinnes2017} usado en la
Fase 5 permite, a diferencia de k-means, un número de clusters no fijado a
priori y una categoría explícita de ruido para conductor-temporadas
atípicos (pruebas, sustituciones). La literatura de estilo de conducción en
motorsport de alto rendimiento es comparativamente escasa; este trabajo
extiende el paradigma de perfilado no supervisado a telemetría de
competición de 10–20\,Hz con una covariable explícita de evolución de pista.

\subsection{Arquitecturas de datos para telemetría a gran escala}

La arquitectura de tres capas Bronze/Silver/Gold usada aquí sigue el patrón
de \emph{lakehouse}, formalizado académicamente por
Armbrust~\emph{et al.}~\cite{armbrust2021}, que argumenta que los formatos
abiertos (Parquet) con soporte de primera clase para cargas de trabajo de ML
superan a los almacenes de datos tradicionales para este tipo de análisis.
DuckDB~\cite{raasveldt2019} habilita la ejecución de agregaciones SQL fuera
de núcleo directamente sobre los 325.5 millones de filas de Bronze sin un
clúster distribuido. Esto contrasta con gran parte de la literatura reciente
de análisis de F1 basada en FastF1, que opera con \texttt{pandas} sobre una
sola sesión o carrera a la vez —por ejemplo, el trabajo de predicción de
pit-stops con \emph{deep learning} de~\cite{frontiers2025pitstop} entrena
sobre telemetría extraída sesión por sesión sin una capa de almacenamiento
persistente ni particionado jerárquico, lo que dificulta escalar a múltiples
temporadas. La arquitectura aquí presentada demuestra que ese salto de
escala (57\,GB, 5 temporadas) es viable en hardware de consumidor sin
infraestructura distribuida.



\subsection{Conjunto de datos}

El conjunto de datos proviene de la fuente pública
\textbf{TracingInsights}~\cite{tracinginsights} (licencia Apache-2.0),
que a su vez extrae de FastF1~\cite{fastf1}, Ergast/Jolpica~\cite{jolpica}
y OpenF1. Cubre las temporadas 2021–2025, con 22–23 Grandes Premios por
temporada y hasta sesiones de prácticas, clasificación, sprint y carrera.

Los datos se almacenan en una jerarquía:
\texttt{temporada/GP/sesión/piloto/vuelta\_tel.json}. Cada archivo de
telemetría contiene muestras de alta frecuencia ($\sim$10–20\,Hz) con
velocidad, acelerador, freno, marcha, RPM, DRS, aceleraciones triaxiales,
coordenadas de pista, distancia al coche de adelante y más. Además existen
archivos de tiempos de vuelta, meteorología, geometría de curvas y mensajes
de dirección de carrera.

Antes de construir el ETL definitivo se ejecutó una fase de validación de
esquema (\texttt{src/ingest/explore.py}) sobre una muestra acotada
(máx. 2 GPs $\times$ 2 conductores $\times$ 2 vueltas por tipo de sesión y
año), ya que el esquema real varía entre temporadas: la telemetría viene
envuelta en una clave \texttt{"tel"}, algunos canales
(\texttt{rel\_distance}, \texttt{DistanceToDriverAhead}, \texttt{acc\_*})
no están presentes en todos los años, y los valores nulos se codifican
como la cadena literal \texttt{"None"} en lugar de \texttt{null} de JSON.
Este manifiesto de esquema (\texttt{src/ingest/schema.py}) es el contrato
que usa el ETL para normalizar tipos y rellenar canales faltantes con
literales nulos tipados.

\begin{table}[!htbp]
\centering
\caption{Resumen de almacenamiento por temporada.}
\label{tab:resumen}
\begin{tabular}{@{}cccc@{}}
\toprule
\textbf{Año} & \textbf{GPs} & \textbf{Archivos JSON} & \textbf{Tamaño} \\
\midrule
2021 & 22 & 62{,}996 & 11.27\,GB \\
2022 & 22 & 62{,}999 & 10.21\,GB \\
2023 & 22 & 62{,}608 & 10.18\,GB \\
2024 & 23 & 70{,}668 & 11.65\,GB \\
2025 & 23 & 106{,}663 & 13.74\,GB \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Lago de datos (Lakehouse)}

La arquitectura de datos sigue el patrón Bronze $\to$ Silver $\to$ Gold:

\begin{itemize}
  \item \textbf{Bronze} (\texttt{data/bronze/telemetry/}): filas crudas de
        telemetría particionadas por temporada en formato Parquet con ZSTD.
        325.5 millones de filas, 16.7\,GB en disco.
  \item \textbf{Silver} (\texttt{data/silver/}): stints agregados (laptime,
        compound, vida), eventos de carrera (meteorología, curvas, mensajes
        de dirección). 0.04\,GB.
  \item \textbf{Gold} (\texttt{data/gold/}): tabla maestra de
        características por vuelta ($\sim$317\,000 filas), perfil de estilo
        por conductor-temporada, curvas de degradación por stint,
        vectores de micro-sectores y embeddings. $\sim$0.5\,GB.
\end{itemize}

\subsection{ETL de streaming}

El ETL (\texttt{src/ingest/etl.py}) procesa las sesiones en paralelo usando
\texttt{multiprocessing.Pool} con contexto de `spawn'. Cada worker procesa
una sesión completa: carga un archivo de telemetría a la vez, lo normaliza
(manejo de nulos tipo \texttt{"None"}, alineación de canales con longitudes
desiguales, casteo de tipos), y escribe un archivo Parquet por sesión.
Se procesan 585 sesiones en $\sim$11 minutos con 8 workers, sin exceder
los 32\,GB de RAM. El pipeline captura las excepciones por vuelta y por
sub-paso (telemetría, laptimes, eventos de carrera) para que un archivo
corrupto no aborte la sesión completa; cada worker retorna un contador de
errores (\texttt{err}) por sesión.

\subsection{Diagnóstico y recuperación de errores}

De las 585 sesiones, 6 fallaron en la corrida completa (las tres sesiones
de \emph{Pre-Season Testing} 2024/2025 y la \emph{Sprint Shootout} de
São Paulo 2023). En lugar de reintentar a ciegas, se construyó un script
de diagnóstico dedicado (\texttt{src/ingest/diagnose\_errors.py}) que
re-ejecuta \emph{solo} esas sesiones en el proceso principal (sin
\texttt{multiprocessing}), replicando cada sub-paso del ETL
(parseo de telemetría, concatenación Silver, unión con laptimes, tablas
de eventos) con \emph{tracebacks} completos en vez del contador \texttt{err=1}
silenciado por el pool de workers. Esto permitió aislar la causa exacta
(estructura de carpetas atípica en pruebas de pretemporada y sesiones
cortas de shootout) y reingerir únicamente las sesiones afectadas con
la bandera \texttt{-{}-fix-failed}, sin necesidad de rehacer las 579
sesiones ya procesadas correctamente.

% ============================================================================
% 3. INGENIERÍA DE CARACTERÍSTICAS (GOLD)
% ============================================================================
\section{Ingeniería de Características}

El almacén Gold se construye en tres tablas principales:

\subsection{Características por vuelta (\texttt{lap\_features})}

Una fila por vuelta ($\sim$317\,000), combinando:

\begin{itemize}
  \item \textbf{Telemetría agregada}: estadísticas de velocidad, acelerador,
        freno, DRS, G-force (media, desviación estándar, máximo), integrales
        de carga lateral y longitudinal ($\sum |a_{\text{lat}}| \cdot d$),
        fracción de aire sucio (\texttt{DistanceToDriverAhead} $< 10$\,m).
  \item \textbf{Corrección de combustible}: en Carreras y Sprints,
        $t_{\text{corr}} = t_{\text{lap}} + 0.03 \times \text{lap}$ para
        aislar la degradación del neumático del efecto del combustible.
  \item \textbf{Características rolling causales}: ventanas en $w \in
        \{3, 5, 8\}$ vueltas para media, desviación estándar, mediana,
        rango min-max, pendiente de 5 vueltas y EMA con
        $\alpha \in \{0.2, 0.5\}$. Gap a la mejor vuelta acumulada
        del stint (\texttt{pace\_above\_best}).
  \item \textbf{Delta de sectores}: brechas entre sectores S1/S2/S3,
        degradación acumulada por sector desde el inicio del stint.
  \item \textbf{Contexto ambiental}: temperatura de pista/ambiente, lluvia,
        viento, proxy de evolución de la pista con lavado por lluvia
        ($\gamma_{\text{wash}} = 0.3$, eficiencia húmeda 0.5).
\end{itemize}

Las estadísticas de telemetría se calculan desde el Bronze usando DuckDB
(conexión local, ventanas SQL), lo que permite procesar 325.5 millones de
filas fuera de núcleo por temporada sin cargárlas en RAM.

\subsection{Perfil de estilo de conducción (\texttt{driver\_style})}

Una fila por conductor-temporada con medias y desviaciones estándar de
todas las métricas de telemetría. Se usa como covariable en el optimizador.

\subsection{Curvas de degradación por stint (\texttt{stint\_degradation})}

Una fila por stint ($\sim$50\,000) con pendiente de degradación, presencia
de cliff, carga mecánica y condiciones ambientales. Campos de resultado
(\texttt{stint\_length}, \texttt{pace\_end}) marcados como \textbf{leakage}
para el modelo RUL.

% ============================================================================
% 4. MODELO 1: PREDICCIÓN DE RUL Y CLIFF
% ============================================================================
\section{Modelo 1: Predicción de Vida Útil Remanente y Cliff}

\subsection{Etiquetado}

El punto clave es definir cuándo comienza el `cliff' de degradación de un
stint. Se usa una regla de comportamiento físico:

\begin{enumerate}
  \item El \emph{mejor ritmo del stint} es el mínimo del laptime corregido
        por combustible dentro del stint completo (usa información futura
        solo del lado de la etiqueta, nunca de las características de
        entrada).
  \item El \textbf{cliff} ($T_{\text{end}}$) es la primera vuelta, a partir
        de la posición 3 del stint (para que la vuelta de salida de boxes
        no pueda dispararlo), en la que la \emph{mediana rolling} de 3
        vueltas del ritmo corregido cruza sostenidamente (3 vueltas
        consecutivas) el mejor ritmo del stint $+\ 1.5$\,s. La mediana
        rolling es robusta a vueltas aisladas de tráfico o vueltas de
        entrada.
  \item La \textbf{RUL objetivo} es
        $\min(T_{\text{end}} - \text{vida}, C_{\text{compuesto}})$ con
        $C = \{15, 22, 30\}$ vueltas para SOFT/MEDIUM/HARD (recorte por
        tramos); los stints censurados (sin cliff observado) usan
        $\text{fin del stint} + 1 - \text{vida}$.
  \item El \textbf{cliff\_within\_3} (clasificación) indica si el neumático
        ya está en el cliff o entra en él dentro de las próximas 3 vueltas
        ($T_{\text{end}} - \text{vida} \le 3$).
\end{enumerate}

\subsection{Filtrado de stints}

Se eliminan stints truncados por Safety Car/VSC, stints con $<5$ vueltas
en seco, y stints con $>30\%$ de vueltas nulas. El filtrado elimina
$\sim$15\% de los datos totales pero mejora significativamente la calidad
de las etiquetas.

\subsection{Exclusión de fuga de información}

Todas las características derivadas del cliff (\texttt{cliff\_flag},
\texttt{stint\_best\_pace}, \texttt{stint\_laptime\_median}, etc.) se
excluyen estrictamente de la matriz de entrada \textbf{X}. Solo quedan
características de telemetría pura, tendencias de ritmo y entorno. Esto
revela que el F1 previo de 0.997 del clasificador era memorización de la
bandera, y el real (post-leakage) es 0.747.

\subsection{Pérdida asimétrica}

Se implementa una función de pérdida personalizada para XGBoost y LightGBM
que penaliza 2.5 veces más las predicciones tardías (sobreestimar la vida
del neumático es el error costoso):
\begin{equation}
  L(y, \hat{y}) = \begin{cases}
    (y - \hat{y})^2 & \text{si } \hat{y} \le y \\
    2.5 \cdot (\hat{y} - y)^2 & \text{si } \hat{y} > y
  \end{cases}
\end{equation}

Se reporta también la métrica asimétrica NASA PHM con parámetros
$a_1 = 10$, $a_2 = 5$.

\subsection{Resultados (test = 2025, fuera de muestra)}

La partición temporal es: entrenamiento 2021–2023, validación 2024,
test 2025.

\begin{table}[!htbp]
\centering
\caption{RUL regresión (vueltas). Mejor modelo en zona crítica: LSTM; producción: ensamble continuo por tramos.}
\label{tab:rul}
\begin{tabular}{lrrrr}
\toprule
\textbf{Modelo} & \textbf{MAE} & \textbf{RMSE} & $R^2$ & \textbf{MAE$_{\le 10}$} \\
\midrule
Naïve (media por compuesto) & 6.77 & 8.31 & 0.10 & 4.79 \\
XGBoost (asimétrico) & 4.80 & 7.06 & 0.35 & 2.51 \\
XGBoost (simétrico ref.) & 4.70 & 6.85 & 0.39 & 2.70 \\
LSTM (asimétrico) & 5.85 & 8.99 & $-0.06$ & \textbf{2.08} \\
\textbf{Piecewise Blend (Producción)} & 4.95 & 7.73 & 0.22 & \textbf{2.20} \\
\bottomrule
\end{tabular}
\end{table}

El LSTM ofrece mayor sensibilidad en la zona de degradación crítica (RUL $\le 10$ vueltas),
mientras que XGBoost proporciona estabilidad macroscópica evitando el sesgo de compresión
hacia cero del LSTM en fases tempranas del stint. En producción (\texttt{optimizer.py}), se
implementa una función de mezcla continua por tramos $w(\text{RUL}_{\text{xgb}})$ que asigna
peso total al LSTM cuando $\text{RUL}_{\text{xgb}} \le 6$, peso total a XGBoost cuando
$\text{RUL}_{\text{xgb}} \ge 12$, e interpola linealmente en el intervalo intermedio. La
arquitectura LSTM usa embeddings para compuesto y tipo de sesión, 2 capas de 96 unidades,
dropout 0.4 y pérdida asimétrica SmoothL1.

El \textbf{clasificador de cliff} (ensamble XGB+LightGBM con umbral
afinado en validación con selección recall-biased) logra
$F_1 = 0.747$, AUC $= 0.889$ sobre 2025.

% ============================================================================
% 5. MODELO 2: PROFILER DE ESTILO DE CONDUCCIÓN
% ============================================================================
\section{Modelo 2: Profiler de Estilo de Conducción}

\subsection{Preprocesamiento}

Se aplica un filtrado estricto: se eliminan vueltas con laptime nulo,
vueltas de entrada/salida de boxes (primera/última del stint), vueltas
fuera del 107\% del mejor de la sesión, y vueltas bajo Safety Car/VSC.
El filtrado elimina $\sim$44\% de las vueltas, dejando solo las
\emph{hot laps} representativas del ritmo real.

\subsection{Vectores de micro-sectores (Opción B)}

En lugar de entrenar el autoencoder sobre 325 millones de filas crudas,
cada vuelta se codifica como un vector de longitud fija: 10 bins de
distancia normalizados $\times$ 4 canales (G-lateral máximo, throttle medio,
frenado medio, velocidad media) = 40 dimensiones. Se agregan covariables
de evolución de la pista, temperatura, compound y tipo de sesión para
un total de 55 entradas.

\subsection{Evolución de la pista}

Se construye un proxy de evolución de la pista
(\texttt{track\_evolution\_index}) que modela la acumulación de caucho
a lo largo del fin de semana, con:
\begin{itemize}
  \item Pesos por sesión: Práctica 1 (0.6), Práctica 2 (0.8),
        Práctica 3 (1.0), Sprint (1.3), Carrera (1.5).
  \item Lavado por lluvia: sesiones húmedas multiplican el
        estado acumulado por $\gamma_{\text{wash}} = 0.3$.
  \item Factor instantáneo de lluvia: $\times 0.6$ en vueltas con
        neumáticos intermedios/blandos.
\end{itemize}

\subsection{Autoencoder y clustering}

El autoencoder (GPU) tiene arquitectura 55$\to$128$\to$64$\to$16
(latent)$\to$64$\to$128$\to$55 con activaciones GELU y BatchNorm.
Se entrena en 2021–2024 (90/10 early stop, paciencia 8) y evalúa
generalización en 2025.

Los embeddings latentes de cada conductor-temporada se agrupan con
HDBSCAN (minimum cluster size = 8):

\begin{itemize}
  \item \textbf{Cluster 0} (112 conductor-temporadas): mayor carga
        de frenado en curvas (G-lateral 36.8) pero la degradación
        corregida por combustible más suave ($-0.05$\,s/vuelta) ---
        eficiencia de frenado de élite.
  \item \textbf{Cluster 1} (28, mayormente novatos/pruebas): entradas
        más suaves pero degradación más pronunciada ($-0.18$\,s/vuelta).
\end{itemize}

Silueta = 0.69. El error de reconstrucción en 2025 (fuera de muestra)
es media $= 0.00203$, p95 $= 0.00491$.

% ============================================================================
% 6. MODELO 3: OPTIMIZADOR DE VENTANA DE PARADA
% ============================================================================
\section{Modelo 3: Optimizador de Estrategia Prescriptiva}

\subsection{Modelo de degradación}

Se ajusta sobre carreras 2021–2024 (2025 excluido para backtest):
\begin{itemize}
  \item \textbf{Beta} por compuesto (pendiente media corregida por
        combustible): $\beta_S = 0.107 > \beta_M = 0.076 >
        \beta_H = 0.045$\,s/vuelta.
  \item \textbf{Offset por cluster}: ajuste de estilo por clusters
        HDBSCAN.
  \item \textbf{Coeficiente de temperatura}: cov(beta, temp)/var(temp).
  \item \textbf{Sigma robusta}: basada en IQR (la desviación estándar
        bruta está destruida por outliers de tráfico).
  \item \textbf{Distribución empírica de cliff}: cuantiles + probabilidad
        de censura por compuesto.
  \item \textbf{Offsets de ritmo por compuesto}: SOFT-MEDIUM medido
        pareado por conductor ($-0.35$\,s), HARD simétrico
        (+0.35\,s) para evitar contaminación de evolución de pista.
\end{itemize}

\subsection{Simulador Monte Carlo}

El evaluador calcula el tiempo total de un stint de $L$ vueltas
analíticamente (sin simular vuelta por vuelta):
\begin{equation}
  T_{\text{stint}} = L(b + p) + \beta \frac{L(L+1)}{2} + T_{\text{cliff}}
\end{equation}
donde $b$ es el ritmo base, $p$ la penalización del escenario,
$\beta$ la pendiente de degradación, y $T_{\text{cliff}}$ el costo
adicional si ocurre un cliff durante el stint.

Se enumeran estrategias de 0/1/2 paradas con la regla de dos compuestos
de la F1. Cada estrategia se evalúa con $N = 400$ simulaciones Monte
Carlo, generando distribuciones con bandas p5–p95.

\subsection{Escenarios}

Se modelan tres contextos de carrera:
\begin{itemize}
  \item \textbf{Aire limpio}: $\beta \times 1.00$, penalización 0\,s.
  \item \textbf{Tren DRS}: $\beta \times 1.15$, penalización +0.25\,s/vuelta.
  \item \textbf{Backmarker}: $\beta \times 1.25$, penalización +0.50\,s/vuelta.
\end{itemize}

\subsection{Monitor de parada en carrera}

El monitor aplica reglas compuestas lap a lap:
\begin{enumerate}
  \item \textbf{Filtrado anti-falso-positivo}: la probabilidad de cliff
        debe superar el umbral en $\ge 2$ vueltas consecutivas
        (debouncing).
  \item \textbf{Regla compuesta cliff/RUL}: si el cliff dispara pero
        RUL predice $>15$ vueltas restantes, se clasifica como
        degradación térmica transitoria (ej. aire sucio sobrecalentando
        el neumático) y \textbf{no se recomienda parada}.
  \item \textbf{RUL inminente}: si RUL $\le 2$ vueltas, se recomienda
        parada inmediata independientemente del cliff.
\end{enumerate}

% ============================================================================
% 7. EVALUACIÓN Y RESULTADOS
% ============================================================================
\section{Evaluación: Backtest 2025}

\subsection{Diseño}

En cada punto de decisión real (final del stint 1) de 19 carreras secas
de 2025, se evalúa la estrategia restante ejecutada y la recomendada por
el optimizador a través del \textbf{mismo} simulador Monte Carlo con
números aleatorios pareados (los parámetros de degradación se reajustan
solo sobre 2021–2024). El delta es
$\Delta = \text{media(real)} - \text{media(recomendada)}$; $\Delta > 0$
indica que el optimizador gana.

\subsection{Resultados}

\begin{table}[!htbp]
\centering
\caption{Backtest pareado Monte Carlo (2025, aire limpio).}
\label{tab:backtest}
\begin{tabular}{lr}
\toprule
Decisiones evaluadas & 313 \\
Tasa de victoria del optimizador & \textbf{78.6\%} \\
$\Delta$ mediana (real $-$ recomendada) & $+15.0$\,s \\
$\Delta$ media / p10 / p90 & $+2.1$ / $-76.2$ / $+37.9$\,s \\
Paradas promedio: real vs recomendada & 1.66 vs 1.33 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Ablaciones}

Se evaluó la sensibilidad eliminando componentes del modelo (sobre una
submuestra de 120 decisiones):
\begin{itemize}
  \item \textbf{Sin modelo de cliff}: la recomendación cambia en 99/120
        decisiones, con pérdida media de 12.3\,s.
  \item \textbf{Sin offsets de compuesto}: cambia en 17/120.
  \item \textbf{Sin clusters de estilo}: cambia en 0/120 (el efecto del
        estilo es capturado por otros canales en la práctica).
\end{itemize}

\subsection{Mitigación parcial del sesgo de selección}

El backtest comparte el simulador entre el recomendador y el evaluador, lo
que introduce un sesgo de selección a favor del modelo. Se realizaron dos
análisis complementarios para cuantificarlo:

\begin{enumerate}
  \item \textbf{Estratificación por divergencia de paradas}: las 313
        decisiones se separan en función de si el número de paradas
        recomendado coincide con el real. En las decisiones donde el
        optimizador propone el mismo número de paradas que el piloto
        (el caso donde la ventaja es ``honesta''), la tasa de victoria y
        la mediana del delta se informan por separado de las decisiones
        divergentes.
  \item \textbf{Ritmo realizado 2025}: para las carreras cuyo desenlace es
        observable, se re-puntúa la estrategia recomendada usando los tiempos
        de vuelta \emph{realizados} (en lugar del simulador) sustituyendo solo
        el ritmo de estado estacionario del compuesto. Esto elimina la
        circularidad simulador-a-simulador: la ventaja mediana cae a
        $\approx$0\,s (tasa de victoria $\approx$47\%), revelando que gran
        parte del margen reportado de 78.6\% es artefacto de compartir la
        física de degradación.
\end{enumerate}

\subsection{Análisis de sensibilidad del umbral de cliff}

Se barrió el umbral de cliff (\texttt{CLIFF\_THR} $\in \{1.0,1.5,2.0\}$\,s)
contra la ventana de sostenimiento ($\in \{2,3,4\}$ vueltas) midiendo el
efecto sobre la generación de etiquetas y el rendimiento del clasificador
(tabla~\ref{tab:sensitivity}). Etiquetas más conservadoras (umbral alto,
ventana larga) reducen la tasa de cliff observado y desplazan la RUL mediana
hacia arriba, intercambiando recall por precisión.

\begin{table}[!htbp]
\centering
\caption{Sensibilidad del etiquetado y del clasificador de cliff.}
\label{tab:sensitivity}
\small
\begin{tabular}{cc>{$}r<{4}c>{$}c<{4}c}
\toprule
\textbf{Umbral (s)} & \textbf{Ventana} & \textbf{cliff cap.} & \textbf{pos.} & \textbf{RUL p50} & \textbf{F$_1$} \\
\midrule
1.0 & 2 & 0.805 & 0.689 & 0 & — \\
1.0 & 3 & 0.701 & 0.604 & 0 & — \\
1.0 & 4 & 0.582 & 0.523 & 1 & — \\
1.5 & 2 & 0.608 & 0.507 & 2 & — \\
1.5 & 3 & 0.488 & 0.413 & 4 & 0.747 \\
2.0 & 2 & 0.464 & 0.397 & 4 & — \\
2.0 & 3 & 0.349 & 0.306 & 6 & — \\
2.0 & 4 & 0.251 & 0.238 & 8 & — \\
\bottomrule
\end{tabular}
\end{table}

La configuración por defecto (1.5\,s, ventana 3) se sitúa en un punto
intermedio del barrido: relajar el umbral a 1.0\,s triplica la tasa de
cliff, mientras que endurecerlo a 2.0\,s con ventana 4 la reduce a menos
de la mitad, lo que reafirma que los resultados reportados son sensibles a
definiciones de cliff más conservadoras.

% ============================================================================
% 8. LIMITACIONES Y TRABAJO FUTURO
% ============================================================================
\section{Limitaciones y Trabajo Futuro}

\subsection{Comparación con el estado del arte}

La tabla~\ref{tab:sota} sitúa este trabajo frente a los estudios más
cercanos discutidos en la Sección~II, a lo largo de los ejes donde la
comparación es más informativa.

\begin{table*}[!htbp]
\centering
\caption{Comparación cualitativa con trabajos relacionados.}
\label{tab:sota}
\small
\begin{tabular}{@{}p{2.6cm}p{3cm}p{2.6cm}p{2.6cm}p{2.6cm}@{}}
\toprule
 & \textbf{Este trabajo} & \textbf{Aguad \& Thraves~\cite{aguad2024}} &
\textbf{Cappello \& Hoegh~\cite{cappello2025tire}} &
\textbf{Frontiers~\cite{frontiers2025pitstop}} \\
\midrule
Escala de datos & 5 temporadas, 325.5M filas de telemetría & tiempos de vuelta, una temporada & 1 carrera, solo tiempos de vuelta & 1 sesión, telemetría cruda \\
Modelo de oponentes & No & Sí (juego de Stackelberg) & No & No \\
Cuantificación de incertidumbre & Monte Carlo empírico (bandas IQR) & determinista / estocástico simple & posterior bayesiana completa & clasificación puntual (sin bandas) \\
Backtest fuera de muestra & Sí (313 decisiones reales, 2025) & simulación interna & No reportado & hold-out simple \\
Adaptación en carrera & Reglas de debounce + RUL por vuelta & No & No & clasificación por vuelta \\
\bottomrule
\end{tabular}
\end{table*}

Del contraste se desprenden limitaciones concretas de este trabajo:

\begin{enumerate}
  \item \textbf{Ausencia de modelado de oponentes}: a diferencia de
        Aguad y Thraves~\cite{aguad2024}, el optimizador Monte Carlo evalúa
        cada estrategia de forma aislada, sin representar el efecto de
        adelantamientos, tráfico o reacciones estratégicas de otros
        equipos. Incorporar un componente de juego (aunque sea una
        heurística de posición relativa) es la limitación más señalada
        frente a la literatura de teoría de juegos en pit stops.
  \item \textbf{Cuantificación de incertidumbre menos principiada}: las
        bandas p5–p95 del simulador Monte Carlo se derivan de dispersión
        empírica robusta (IQR) y distribuciones empíricas de cliff, no de
        una posterior bayesiana completa como en~\cite{cappello2025tire}.
        Esto simplifica el cómputo pero no propaga correctamente la
        incertidumbre de los propios parámetros ajustados ($\beta$,
        $\sigma$, offsets de cluster).
  \item \textbf{Estrategia estática, no reactiva en tiempo real}: a
        diferencia de los enfoques de aprendizaje por
        refuerzo~\cite{neumann2025learning,thomas2025rl}, que permiten
        inferencia rápida y adaptación continua durante la carrera, el
        optimizador aquí recalcula la estrategia completa por invocación
        y no aprende una política; el monitor de parada (Fase 6) compensa
        parcialmente esto con reglas de alerta lap-a-lap.
\end{enumerate}

Las limitaciones adicionales, ya identificadas internamente durante el
desarrollo, son:

\begin{enumerate}
  \item \textbf{Sesgo de selección}: el recomendador y el evaluador
        comparten el mismo simulador; las carreras contrfactuales reales
        son unknowables.
  \item \textbf{Tendencia a una parada}: el optimizador promedia 1.33
        paradas vs 1.66 reales, sugiriendo que el modelo de degradación
        subestima el costo acumulado de stints largos.
  \item \textbf{Pérdida de parada constante}: se asume 21\,s para todos
        los circuitos; una estimación específica por pista usando longitud
        del carril de boxes mejoraría la precisión.
  \item \textbf{Escenarios como priors}: los multiplicadores de tren DRS
        y backmarker no se ajustan de los datos.
  \item \textbf{Exclusión de carreras con lluvia}: el modelo es seco
        únicamente.
  \item \textbf{$R^2$ moderado}: la RUL física tiene un techo
        $R^2 \approx 0.39$ tras la remoción de fugas (el valor previo
        de 0.61 estaba confundido por estrategia).
\end{enumerate}

Entre las líneas de trabajo futuro se incluyen: estimación dinámica de
pérdida de parada por circuito, incorporación de datos de neumáticos
(temperatura de banda, presión), un modelo wet-climate, y un dashboard
en tiempo real para visualización de recomendaciones durante la carrera.

% ============================================================================
% 9. CONCLUSIONES
% ============================================================================
\section{Conclusiones}

Se ha construido un motor de estrategia prescriptiva completo, reproducible
y ejecutable en un portátil: desde JSON crudo hasta recomendaciones de
parada rankeadas con cuantificación de incertidumbre, que superan las
estrategias reales de 2025 en $\sim$79\% de los puntos de decisión bajo
la física de degradación ajustada.

El pipeline demuestra que es posible procesar datos de tipo ``big data''
($\sim$57\,GB, 325.5M filas) en un entorno personal con la combinación
adecuada de herramientas: DuckDB para consultas fuera de núcleo, Polars
para ETL de streaming, Parquet comprimido con ZSTD para almacenamiento,
y GPU para modelado. La arquitectura de lago de datos en tres capas
(Bronze/Silver/Gold) garantiza trazabilidad y reproducibilidad.

Todos los código, métricas y anotaciones de fase están versionados en
Git; el runbook (\texttt{RUNBOOK.md}) ejecuta el pipeline completo
extremo a extremo con un solo comando.

% ----------------------------------------------------------------------------
% Referencias
% ----------------------------------------------------------------------------
\begin{thebibliography}{1}
\bibitem{tracinginsights}
TracingInsights, ``F1 telemetry datasets,'' 2025. [En línea]. Disponible:
\url{https://tracinginsights.com/data/}.
\bibitem{fastf1}
theOehrly, ``FastF1: A python package for accessing and analyzing Formula 1
results, schedules, timing data and telemetry.'' [En línea]. Disponible:
\url{https://github.com/theOehrly/FastF1}.
\bibitem{jolpica}
Jolpica, ``Jolpica F1 (Ergast) — An open F1 API.'' [En línea]. Disponible:
\url{https://github.com/jolpica/jolpica-f1}.
\bibitem{polars}
Polars, ``Polars: Fast multi-threaded DataFrame library.'' [En línea].
Disponible: \url{https://pola.rs/}.
\bibitem{duckdb}
DuckDB, ``DuckDB: An embedded analytics database.'' [En línea]. Disponible:
\url{https://duckdb.org/}.
\bibitem{xgboost}
Chen, T. y Guestrin, C., ``XGBoost: A scalable tree boosting system,''
\textit{Proc. 22nd ACM SIGKDD}, 2016, pp. 785–794.
\bibitem{lightgbm}
Ke, G. \textit{et al.}, ``LightGBM: A highly efficient gradient boosting
decision tree,''
\textit{Advances in Neural Information Processing Systems}, vol.~30, 2017.
\bibitem{campello2013}
Campello, R. J. G. B., Moulavi, D. y Sander, J., ``Density-based clustering
based on hierarchical density estimates,''
\textit{Proc. 17th Pacific-Asia Conf. on Knowledge Discovery and Data
Mining (PAKDD)}, 2013, pp. 160–172.
\bibitem{mcinnes2017}
McInnes, L., Healy, J. y Astels, S., ``hdbscan: Hierarchical density based
clustering,'' \textit{Journal of Open Source Software}, vol.~2, no.~11,
p.~205, 2017.
\bibitem{pytorch}
Paszke, A. \textit{et al.}, ``PyTorch: An imperative style, high-performance
deep learning library,''
\textit{Advances in Neural Information Processing Systems}, vol.~32, 2019.
\bibitem{aguad2024}
Aguad, F. y Thraves, C., ``Optimizing pit stop strategies in Formula 1 with
dynamic programming and game theory,'' \textit{European Journal of
Operational Research}, vol.~319, no.~3, pp. 908–919, 2024.
\bibitem{carrasco2023}
Carrasco Heine, O. F. y Thraves, C., ``On the optimization of pit stop
strategies via dynamic programming,'' \textit{Central European Journal of
Operations Research}, vol.~31, no.~1, pp. 239–268, 2023.
\bibitem{miqp2024}
``Optimization of pit stop strategies in Formula 1 racing: a data-driven
approach,'' Repositorio Universidad de Chile, 2024. [En línea]. Disponible:
\url{https://repositorio.uchile.cl/handle/2250/199664}.
\bibitem{neumann2025learning}
Neumann, M.-P. \textit{et al.}, ``Towards learning-based Formula 1 race
strategies,'' arXiv:2512.21570, 2025.
\bibitem{thomas2025rl}
Thomas, D., Jiang, J., Kori, A., Russo, A., Winkler, S., Sale, S.,
McMillan, J., Belardinelli, F. y Rago, A., ``Explainable reinforcement
learning for Formula One race strategy,'' \textit{Proc. 40th ACM/SIGAPP
Symposium on Applied Computing}, 2025, pp. 1090–1097.
\bibitem{cappello2025tire}
Cappello, C. y Hoegh, A., ``A state-space approach to modeling tire
degradation in Formula 1 racing,'' arXiv:2512.00640, 2025.
\bibitem{saxena2008}
Saxena, A., Goebel, K., Simon, D. y Eklund, N., ``Damage propagation
modeling for aircraft engine run-to-failure simulation,'' \textit{Proc.
2008 Intl. Conf. on Prognostics and Health Management (PHM)}, 2008, pp.
1–9.
\bibitem{wang2018clustering}
Wang, W., Ramesh, A. y Zhao, D., ``Clustering of driving encounter
scenarios using connected vehicle trajectories,'' arXiv:1807.08415, 2018.
\bibitem{armbrust2021}
Armbrust, M., Ghodsi, A., Xin, R. y Zaharia, M., ``Lakehouse: A new
generation of open platforms that unify data warehousing and advanced
analytics,'' \textit{Proc. 11th Conf. on Innovative Data Systems Research
(CIDR)}, 2021.
\bibitem{raasveldt2019}
Raasveldt, M. y Mühleisen, H., ``DuckDB: An embeddable analytical
database,'' \textit{Proc. 2019 Intl. Conf. on Management of Data (SIGMOD)},
2019, pp. 1981–1984.
\bibitem{frontiers2025pitstop}
``Data-driven pit stop decision support for Formula 1 using deep learning
models,'' \textit{Frontiers in Artificial Intelligence}, 2025.
\end{thebibliography}

\end{document}